'use strict';
import SecurityProgramConfigId from './SecurityProgramConfigId';

export class SecurityProgramConfig {
        id: SecurityProgramConfigId= new SecurityProgramConfigId();
    configName: string = '';
    configValue: string = '';
    constructor(){
    }
}
